# 청년 정책 추천 시스템 Python 의존성 (Updated)
# pip install -r requirements.txt

# Vector Database
# Qdrant 최신 기능(FastEmbed, 비동기 최적화) 지원을 위해 업데이트
qdrant-client>=1.12.0

# OpenAI
# GPT-4o 및 최신 모델, 구조화된 출력(Structured Outputs) 지원
openai>=1.55.0
# 토큰 계산을 위한 라이브러리 (RAG 필수)
tiktoken>=0.8.0

# HTTP 클라이언트 (비동기/동기)
# 보안 패치 및 성능 개선 버전
aiohttp>=3.11.0
requests>=2.32.0

# 환경 변수 및 설정 관리
# Pydantic V2 기반의 설정 관리 (OpenAI 라이브러리와 호환성 강화)
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.6.0

# JSON Schema 검증
jsonschema>=4.23.0

# 테스트
# Pytest 8.x 최신 기능 활용
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# 데이터 처리
# Numpy 2.0 호환성 및 Pandas 최신 성능 개선
pandas>=2.2.0
numpy>=2.1.0

# 웹 크롤링
beautifulsoup4>=4.12.3
# 동적 페이지 크롤링이 필요할 경우를 대비해 playwright 추가 권장 (선택)
# playwright>=1.48.0

# 모니터링 & 시스템 관리
psutil>=6.1.0

# 로깅 & 터미널 UI
rich>=13.9.0